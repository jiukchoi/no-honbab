import styled, { css } from '@emotion/native';
import {
    Text,
    TextInput,
    View,
    Image,
    TouchableOpacity,
    Button,
  } from "react-native";

export const MapContainer = styled.View`
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
`